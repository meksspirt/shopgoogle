# Интеграция настроек в сайт

## Обзор

Все настройки из админ-панели теперь активно используются на сайте.

## Где используются настройки

### 🔥 Высокий приоритет

#### 1. Monobank Link
**Где:** Страница успешного оформления заказа (`/checkout/success`)
- Компонент: `SuccessPageButtons.tsx`
- Кнопка "💳 Оплатити через Monobank" использует ссылку из настроек

#### 2. Email для уведомлений
**Где:** Будущая функция - система email уведомлений
- Планируется: Автоматическая отправка писем о новых заказах

#### 3. Телефон поддержки
**Где:** 
- Navbar (верхнее меню) - `Navbar.tsx`
- Footer (футер) - `Footer.tsx`
- Отображается с иконкой 📞 и кликабельной ссылкой `tel:`

#### 4. Instagram
**Где:**
- Navbar (верхнее меню) - `Navbar.tsx`
- Footer (футер) - `Footer.tsx`
- Отображается с иконкой 📷 и открывается в новой вкладке

#### 5. Минимальная сумма заказа
**Где:** Страница оформления заказа (`/checkout`)
- Компонент: `checkout/page.tsx`
- Блокирует кнопку оплаты если сумма меньше минимальной
- Показывает предупреждение: "Мінімальна сума замовлення: X грн"

### 📋 Средний приоритет

#### 6. Бесплатная доставка от
**Где:** Страница оформления заказа (`/checkout`)
- Компонент: `checkout/page.tsx`
- Показывает "Безкоштовно 🎉" если сумма достигнута
- Показывает подсказку: "Додайте ще X грн для безкоштовної доставки"

#### 7. Сообщение об успешном заказе
**Где:** Страница успешного оформления (`/checkout/success`)
- Компонент: `checkout/success/page.tsx`
- Заменяет стандартный текст "Дякуємо за покупку" на кастомное сообщение

#### 8. Условия доставки
**Где:** Будущая страница `/delivery`
- Планируется: Отдельная страница с условиями доставки

#### 9. О магазине
**Где:** Будущая страница `/about`
- Планируется: Страница "О нас"

#### 10-11. Telegram настройки
**Где:** Будущая функция - Telegram уведомления
- Планируется: Автоматическая отправка уведомлений в Telegram

### ⚙️ Низкий приоритет

#### 12-14. SEO настройки
**Где:** Будущая интеграция в `layout.tsx`
- Планируется: Динамические meta теги
- Требует серверного рендеринга или API route

#### 15-18. Юридическая информация
**Где:** 
- Footer (название компании) - `Footer.tsx`
- Будущие страницы: `/terms`, `/privacy`

### 🏪 Магазин

#### 19. Название магазина
**Где:** Footer - `Footer.tsx`
- Отображается в заголовке футера и копирайте

#### 20. Слоган
**Где:** Будущая интеграция на главной странице

#### 21. Режим работы
**Где:** Footer - `Footer.tsx`
- Отображается с иконкой 🕐

#### 22. Товаров на странице
**Где:** Будущая интеграция в каталог
- Планируется: Пагинация каталога товаров

## Примеры использования

### Navbar с настройками

```typescript
// src/components/Navbar.tsx
const [phone, setPhone] = useState('');
const [instagram, setInstagram] = useState('');

useEffect(() => {
    const fetchSettings = async () => {
        const { data } = await supabase
            .from('settings')
            .select('*')
            .in('key', ['support_phone', 'instagram_link']);
        // ... обработка данных
    };
    fetchSettings();
}, []);
```

### Checkout с валидацией

```typescript
// src/app/checkout/page.tsx
const [minOrderAmount, setMinOrderAmount] = useState(0);
const [freeDeliveryFrom, setFreeDeliveryFrom] = useState(0);

// Проверка минимальной суммы
disabled={minOrderAmount > 0 && total < minOrderAmount}

// Отображение бесплатной доставки
{freeDeliveryFrom > 0 && total >= freeDeliveryFrom && (
    <span className="text-success fw-bold">Безкоштовно 🎉</span>
)}
```

### Footer с контактами

```typescript
// src/components/Footer.tsx
const [settings, setSettings] = useState<{ [key: string]: string }>({});

useEffect(() => {
    const fetchSettings = async () => {
        const { data } = await supabase
            .from('settings')
            .select('*')
            .in('key', ['company_name', 'support_phone', 'instagram_link', 'working_hours']);
        // ... обработка данных
    };
    fetchSettings();
}, []);
```

## Планируемые интеграции

### 1. SEO Meta Tags (Высокий приоритет)

Создать API route для получения SEO настроек:

```typescript
// app/api/settings/seo/route.ts
export async function GET() {
    const { data } = await supabase
        .from('settings')
        .select('*')
        .in('key', ['site_title', 'site_description', 'site_keywords']);
    
    return Response.json(data);
}
```

Использовать в layout или metadata:

```typescript
export async function generateMetadata() {
    const res = await fetch('/api/settings/seo');
    const settings = await res.json();
    
    return {
        title: settings.site_title,
        description: settings.site_description,
        keywords: settings.site_keywords
    };
}
```

### 2. Email уведомления (Высокий приоритет)

При создании заказа отправлять email:

```typescript
// После создания заказа
const { data: emailSetting } = await supabase
    .from('settings')
    .select('value')
    .eq('key', 'notification_email')
    .single();

if (emailSetting?.value) {
    await sendEmail({
        to: emailSetting.value,
        subject: `Нове замовлення #${orderId}`,
        body: `...`
    });
}
```

### 3. Telegram уведомления (Средний приоритет)

```typescript
const { data: telegramSettings } = await supabase
    .from('settings')
    .select('*')
    .in('key', ['telegram_bot_token', 'telegram_chat_id']);

if (telegramSettings) {
    await sendTelegramMessage({
        token: telegramSettings[0].value,
        chatId: telegramSettings[1].value,
        message: `Нове замовлення #${orderId}`
    });
}
```

### 4. Страницы контента (Средний приоритет)

Создать страницы:
- `/about` - О магазине
- `/delivery` - Условия доставки
- `/terms` - Договор оферты
- `/privacy` - Политика конфиденциальности

Каждая страница загружает соответствующую настройку из базы.

### 5. Каталог с пагинацией (Низкий приоритет)

```typescript
const { data: perPageSetting } = await supabase
    .from('settings')
    .select('value')
    .eq('key', 'products_per_page')
    .single();

const perPage = parseInt(perPageSetting?.value || '12');

// Использовать в запросе товаров
const { data: products } = await supabase
    .from('products')
    .select('*')
    .range(page * perPage, (page + 1) * perPage - 1);
```

## Тестирование

### Как проверить работу настроек:

1. **Navbar:**
   - Зайдите на главную страницу
   - Проверьте наличие телефона и Instagram в меню

2. **Checkout:**
   - Добавьте товары в корзину
   - Перейдите к оформлению
   - Проверьте сообщения о минимальной сумме и бесплатной доставке

3. **Success Page:**
   - Оформите заказ
   - Проверьте кастомное сообщение
   - Проверьте ссылку на Monobank

4. **Footer:**
   - Прокрутите вниз на любой странице
   - Проверьте контакты и режим работы

## Производительность

### Кэширование настроек

Для улучшения производительности можно добавить кэширование:

```typescript
// Кэш в localStorage
const getCachedSettings = (keys: string[]) => {
    const cached = localStorage.getItem('settings_cache');
    if (cached) {
        const { data, timestamp } = JSON.parse(cached);
        // Кэш действителен 1 час
        if (Date.now() - timestamp < 3600000) {
            return data;
        }
    }
    return null;
};

const setCachedSettings = (data: any) => {
    localStorage.setItem('settings_cache', JSON.stringify({
        data,
        timestamp: Date.now()
    }));
};
```

## Безопасность

### Публичные настройки

Все настройки, используемые на фронтенде, доступны для чтения всем пользователям через RLS политику:

```sql
CREATE POLICY "Allow public read access to settings"
ON public.settings FOR SELECT
TO public
USING (true);
```

### Приватные настройки

Для чувствительных данных (API ключи, токены) используйте переменные окружения `.env.local` вместо таблицы settings.

## Поддержка

При добавлении новых настроек:
1. Добавьте запись в таблицу `settings`
2. Добавьте поле в `SettingsPanel.tsx`
3. Интегрируйте в соответствующий компонент
4. Обновите эту документацию
