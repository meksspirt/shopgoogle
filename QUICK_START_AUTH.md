# ⚡ Быстрый старт - Supabase Auth

## 3 шага до запуска

### 1️⃣ Выполните SQL скрипт

Откройте [Supabase SQL Editor](https://app.supabase.com) и выполните:

```sql
-- Скопируйте весь код из файла: supabase/setup_auth_profiles.sql
```

### 2️⃣ Отключите подтверждение email (для тестирования)

1. Supabase Dashboard → **Authentication** → **Providers**
2. Нажмите на **Email**
3. Отключите "Confirm email"
4. Сохраните

### 3️⃣ Создайте администратора

1. Откройте `/admin/login` в браузере
2. Перейдите на вкладку **"Реєстрація"**
3. Зарегистрируйтесь с вашим email
4. В Supabase SQL Editor выполните:

```sql
UPDATE public.profiles 
SET is_admin = TRUE 
WHERE email = 'ваш-email@example.com';
```

## ✅ Готово!

Теперь войдите через `/admin/login` с вашим email и паролем.

---

## 🔍 Проверка

Убедитесь, что вы администратор:

```sql
SELECT email, is_admin FROM public.profiles;
```

Должно показать `is_admin = true` для вашего email.
