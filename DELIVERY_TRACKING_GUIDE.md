# 📦 Автоматичне відстеження доставки

## Огляд

Система автоматично відстежує статус доставки через API Нової Пошти та оновлює статуси замовлень.

---

## 🚀 Функції

### 1. Автоматична зміна статусу при додаванні ТТН

Коли адміністратор вводить та зберігає номер ТТН:
- ✅ Статус замовлення автоматично змінюється на **"Відправлено"**
- ✅ ТТН зберігається в базі даних
- ✅ Клієнт може відстежувати посилку в своєму профілі

### 2. Перевірка статусу окремого замовлення

Для замовлень зі статусом "Відправлено":
- 🔍 Кнопка **"Перевірити статус"** поруч з ТТН
- 📊 Отримання актуального статусу з Нової Пошти
- ✅ Автоматичне оновлення до "Доставлено" якщо посилка отримана

### 3. Масова перевірка всіх доставок

Кнопка **"📦 Перевірити доставки"** в шапці адмінки:
- 🔄 Перевіряє всі замовлення зі статусом "Відправлено"
- 📈 Показує скільки замовлень перевірено та оновлено
- ⚡ Автоматично оновлює статуси доставлених замовлень

---

## 🛠️ Налаштування

### Крок 1: Отримати API ключ Нової Пошти

1. Увійдіть на [my.novaposhta.ua](https://my.novaposhta.ua/)
2. Перейдіть в **Налаштування** → **API ключі**
3. Створіть новий API ключ або скопіюйте існуючий

### Крок 2: Додати ключ в .env.local

```env
# Nova Poshta API Key
NOVA_POSHTA_API_KEY=ваш_api_ключ_тут

# Secret для захисту cron endpoint
CRON_SECRET=ваш_випадковий_секретний_ключ
NEXT_PUBLIC_CRON_SECRET=ваш_випадковий_секретний_ключ
```

### Крок 3: Перезапустити сервер

```bash
npm run dev
```

---

## 📋 Як використовувати

### Для адміністратора

#### Додавання ТТН:
1. Відкрийте панель адміністратора `/admin`
2. Знайдіть замовлення
3. Введіть номер ТТН в поле
4. Натисніть **💾** (Зберегти)
5. ✅ Статус автоматично зміниться на "Відправлено"

#### Перевірка окремого замовлення:
1. Для замовлення зі статусом "Відправлено"
2. Натисніть **🔍** (Перевірити статус)
3. Система покаже актуальний статус
4. Якщо доставлено - статус оновиться автоматично

#### Масова перевірка:
1. Натисніть **"📦 Перевірити доставки"** в шапці
2. Дочекайтеся завершення перевірки
3. Побачите результат: скільки перевірено та оновлено

### Для покупця

1. Увійдіть в профіль `/profile`
2. Перегляньте свої замовлення
3. Якщо є ТТН - натисніть на картку з номером
4. Відкриється сайт Нової Пошти для відстеження

---

## 🔧 API Endpoints

### POST /api/check-delivery-status

Перевіряє статус окремого замовлення.

**Request:**
```json
{
  "trackingNumber": "20450123456789",
  "orderId": "uuid-замовлення"
}
```

**Response:**
```json
{
  "success": true,
  "isDelivered": true,
  "statusCode": "9",
  "statusText": "Отримана",
  "trackingInfo": {
    "number": "20450123456789",
    "status": "Отримана",
    "statusCode": "9",
    "recipientDateTime": "2024-01-15 14:30:00",
    "city": "Київ",
    "warehouse": "Відділення №1"
  }
}
```

### POST /api/update-delivery-statuses

Перевіряє всі відправлені замовлення (для cron job).

**Headers:**
```
Authorization: Bearer your_cron_secret
```

**Response:**
```json
{
  "success": true,
  "message": "Checked 5 orders, updated 2 to delivered",
  "checked": 5,
  "updated": 2,
  "results": [...]
}
```

---

## 📊 Статуси Нової Пошти

Система розпізнає наступні коди статусів як "Доставлено":

| Код | Статус |
|-----|--------|
| 9   | Отримана |
| 10  | Отримана і оплачена |
| 11  | Отримана і оплачена відправнику |
| 106 | Вручена |

Всі інші статуси вважаються "В дорозі".

---

## ⚙️ Автоматизація (Cron Job)

### Налаштування автоматичної перевірки

Для автоматичної перевірки кожні N годин, налаштуйте cron job:

#### Vercel Cron (рекомендовано)

Створіть файл `vercel.json`:

```json
{
  "crons": [
    {
      "path": "/api/update-delivery-statuses",
      "schedule": "0 */6 * * *"
    }
  ]
}
```

Це буде перевіряти доставки кожні 6 годин.

#### Альтернатива: Зовнішній cron сервіс

Використайте сервіси як:
- [cron-job.org](https://cron-job.org)
- [EasyCron](https://www.easycron.com)
- [Uptime Robot](https://uptimerobot.com)

Налаштуйте POST запит на:
```
https://ваш-домен.com/api/update-delivery-statuses
```

З header:
```
Authorization: Bearer ваш_cron_secret
```

---

## 🔒 Безпека

### Захист cron endpoint

Endpoint `/api/update-delivery-statuses` захищений:
- ✅ Потрібен `Authorization` header з секретним ключем
- ✅ Без ключа доступ заборонено
- ✅ Використовує service role key для оновлення БД

### Рекомендації:

1. **Використовуйте складний CRON_SECRET**
   ```bash
   # Згенеруйте випадковий ключ
   openssl rand -base64 32
   ```

2. **Не публікуйте .env.local в git**
   - Файл вже в .gitignore
   - Ніколи не коммітьте секретні ключі

3. **Обмежте rate limiting**
   - API Нової Пошти має ліміти
   - Не викликайте занадто часто

---

## 🐛 Troubleshooting

### Помилка: "Nova Poshta API key not configured"

**Рішення:**
1. Перевірте що `NOVA_POSHTA_API_KEY` в `.env.local`
2. Перезапустіть сервер після додавання ключа

### Помилка: "Failed to get tracking info"

**Можливі причини:**
- Невірний номер ТТН
- ТТН ще не активовано в системі Нової Пошти
- Проблеми з API Нової Пошти

**Рішення:**
- Перевірте номер ТТН на сайті Нової Пошти
- Спробуйте пізніше (ТТН активується не миттєво)

### Статус не оновлюється автоматично

**Рішення:**
1. Перевірте що cron job налаштовано
2. Перевірте логи сервера
3. Спробуйте ручну перевірку кнопкою 🔍

### Помилка: "Unauthorized" при виклику cron endpoint

**Рішення:**
- Перевірте що `Authorization` header правильний
- Переконайтеся що `CRON_SECRET` співпадає в .env та в запиті

---

## 📈 Майбутні покращення

Можливі додаткові функції:

1. **Сповіщення клієнтів**
   - Email при зміні статусу
   - SMS при доставці

2. **Історія статусів**
   - Зберігання всіх змін статусу
   - Відображення timeline доставки

3. **Аналітика**
   - Середній час доставки
   - Статистика по містах
   - Проблемні доставки

4. **Інтеграція з іншими службами**
   - Укрпошта
   - Meest
   - Justin

---

## 📚 Корисні посилання

- [API Нової Пошти - Документація](https://developers.novaposhta.ua/)
- [Tracking Document API](https://developers.novaposhta.ua/view/model/a99d2f28-8512-11ec-8ced-005056b2dbe1/method/a9ae7bc9-8512-11ec-8ced-005056b2dbe1)
- [Коди статусів](https://developers.novaposhta.ua/documentation)

---

## ✅ Чеклист налаштування

- [ ] Отримано API ключ Нової Пошти
- [ ] Додано `NOVA_POSHTA_API_KEY` в .env.local
- [ ] Додано `CRON_SECRET` в .env.local
- [ ] Перезапущено сервер
- [ ] Протестовано додавання ТТН
- [ ] Протестовано перевірку статусу
- [ ] Налаштовано cron job (опціонально)
- [ ] Перевірено що статуси оновлюються

---

Створено: 30 листопада 2025
