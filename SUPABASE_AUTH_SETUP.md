# 🔐 Настройка Supabase Auth для админ-панели

## ✅ Что было сделано

Ваш проект теперь использует **Supabase Auth** - безопасную систему аутентификации уровня Enterprise. Все пароли хэшируются автоматически, а доступ к данным контролируется через RLS (Row Level Security).

---

## 📋 Шаги для запуска

### 1. Настройка базы данных

Выполните SQL скрипт в Supabase SQL Editor:

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Скопируйте содержимое файла `supabase/setup_auth_profiles.sql`
5. Вставьте в редактор и нажмите **Run**

Этот скрипт создаст:
- Таблицу `profiles` для управления ролями пользователей
- RLS политики для защиты данных
- Автоматический триггер для создания профиля при регистрации

### 2. Настройка Supabase Auth

В Supabase Dashboard:

1. Перейдите в **Authentication** → **Providers**
2. Убедитесь, что **Email** провайдер включен
3. **Для тестирования**: отключите "Confirm email" (можно включить позже в продакшене)
4. Сохраните изменения

### 3. Создание администратора

#### Вариант A: Через интерфейс (рекомендуется)

1. Откройте страницу `/admin/login`
2. Перейдите на вкладку **"Реєстрація"**
3. Зарегистрируйтесь с вашим email и паролем
4. Откройте Supabase Dashboard → **SQL Editor**
5. Выполните команду:

```sql
UPDATE public.profiles 
SET is_admin = TRUE 
WHERE email = 'ваш-email@example.com';
```

#### Вариант B: Через Supabase Dashboard

1. Перейдите в **Authentication** → **Users**
2. Нажмите **Add user** → **Create new user**
3. Введите email и пароль
4. После создания, выполните SQL команду выше

### 4. Проверка работы

1. Откройте `/admin/login`
2. Войдите с вашим email и паролем
3. Вы должны попасть в админ-панель

---

## 🔒 Безопасность

### Что защищено:

✅ **Пароли хэшируются** - Supabase использует bcrypt для хэширования паролей
✅ **RLS включен** - Только администраторы могут видеть и изменять данные
✅ **JWT токены** - Сессии защищены через JWT с автоматическим обновлением
✅ **Middleware защита** - Дополнительная проверка на уровне Next.js
✅ **Проверка на клиенте** - useEffect проверяет права при загрузке страницы

### Row Level Security (RLS) политики:

- **orders** - только администраторы могут просматривать, обновлять и удалять заказы
- **products** - только администраторы могут управлять товарами
- **promo_codes** - только администраторы могут управлять промокодами
- **profiles** - пользователи видят только свой профиль, администраторы видят все

---

## 🎯 Как это работает

### Процесс входа:

1. Пользователь вводит email и пароль
2. Supabase Auth проверяет учетные данные
3. Проверяется роль `is_admin` в таблице `profiles`
4. Если пользователь не администратор - доступ запрещен
5. Создается защищенная сессия с JWT токеном

### Защита данных:

```
Запрос к БД → RLS проверяет auth.uid() → Проверяет is_admin → Разрешает/Запрещает
```

Даже если кто-то украдет ваши API ключи, он не сможет получить доступ к данным без валидной сессии администратора.

---

## 🚀 Дополнительные возможности

### Добавление нового администратора:

```sql
-- Сначала пользователь должен зарегистрироваться через /admin/login
-- Затем выполните:
UPDATE public.profiles 
SET is_admin = TRUE 
WHERE email = 'новый-админ@example.com';
```

### Удаление прав администратора:

```sql
UPDATE public.profiles 
SET is_admin = FALSE 
WHERE email = 'пользователь@example.com';
```

### Включение подтверждения email (для продакшена):

1. Supabase Dashboard → **Authentication** → **Providers**
2. Включите "Confirm email"
3. Настройте Email Templates в разделе **Email Templates**

### Добавление Google OAuth (опционально):

1. Supabase Dashboard → **Authentication** → **Providers**
2. Включите **Google**
3. Следуйте инструкциям для настройки Google OAuth
4. Обновите код логина для поддержки Google

---

## 🐛 Решение проблем

### "Доступ заборонено. Тільки для адміністраторів"

Убедитесь, что вы выполнили SQL команду для установки `is_admin = TRUE`:

```sql
SELECT email, is_admin FROM public.profiles;
```

### "Невірний email або пароль"

- Проверьте правильность email и пароля
- Убедитесь, что пользователь зарегистрирован в Supabase Auth

### Middleware редиректит на логин

- Очистите cookies браузера
- Проверьте, что токен сессии сохраняется
- Откройте DevTools → Application → Cookies

---

## 📚 Полезные ссылки

- [Supabase Auth Documentation](https://supabase.com/docs/guides/auth)
- [Row Level Security Guide](https://supabase.com/docs/guides/auth/row-level-security)
- [Next.js Middleware](https://nextjs.org/docs/app/building-your-application/routing/middleware)

---

## ✨ Готово!

Теперь ваша админ-панель защищена на уровне Enterprise. Все пароли хэшируются, данные защищены RLS политиками, а доступ контролируется на нескольких уровнях.
