# Руководство по настройке ссылки на Monobank

## Обзор

В админ-панели добавлен раздел "Налаштування", где можно настроить ссылку на оплату через Monobank. Эта ссылка будет отображаться на странице успешного оформления заказа.

## Настройка базы данных

### 1. Создание таблицы settings

Выполните SQL скрипт в Supabase SQL Editor:

```sql
-- Файл: supabase/create_settings_table.sql
```

Этот скрипт создаст:
- Таблицу `settings` для хранения настроек
- Запись по умолчанию для ссылки на Monobank
- Политики RLS для безопасного доступа

### 2. Проверка создания таблицы

В Supabase Dashboard перейдите в:
- Table Editor → settings
- Убедитесь, что таблица создана и содержит запись с ключом `monobank_payment_link`

## Использование в админке

### Где найти настройки

1. Войдите в админ-панель: `/admin`
2. Прокрутите вниз до раздела **"Налаштування"**
3. Найдите поле **"Посилання на оплату Monobank"**

### Как изменить ссылку

1. Вставьте новую ссылку на Monobank в поле ввода
2. Нажмите кнопку **"Зберегти налаштування"**
3. Появится уведомление об успешном сохранении

### Формат ссылки

Ссылка должна быть в формате:
```
https://send.monobank.ua/jar/XXXXXXXX
```

Где `XXXXXXXX` - это ID вашей банки Monobank.

## Как получить ссылку на банку Monobank

1. Откройте приложение Monobank
2. Перейдите в раздел "Банки"
3. Создайте новую банку или выберите существующую
4. Нажмите "Поделиться"
5. Скопируйте ссылку

## Где используется ссылка

Ссылка отображается на странице успешного оформления заказа:
- URL: `/checkout/success?orderId=XXXXXX`
- Кнопка: **"💳 Оплатити через Monobank"**

## Технические детали

### Компоненты

1. **AdminPage** (`src/app/admin/page.tsx`)
   - Раздел настроек с полем ввода
   - Функции `fetchSettings()` и `saveMonobankLink()`

2. **SuccessPageButtons** (`src/components/SuccessPageButtons.tsx`)
   - Загружает ссылку из базы данных при монтировании
   - Отображает кнопку оплаты с актуальной ссылкой

### База данных

**Таблица:** `settings`

| Поле | Тип | Описание |
|------|-----|----------|
| id | SERIAL | Первичный ключ |
| key | VARCHAR(255) | Уникальный ключ настройки |
| value | TEXT | Значение настройки |
| description | TEXT | Описание настройки |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата обновления |

**Ключ для Monobank:** `monobank_payment_link`

### Политики безопасности (RLS)

- **Чтение:** Доступно всем (public)
- **Запись/Обновление:** Только для аутентифицированных пользователей

## Расширение функционала

В будущем в таблицу `settings` можно добавить другие настройки:
- Email для уведомлений
- Настройки доставки
- Контактная информация
- И т.д.

Просто добавьте новые записи с уникальными ключами и создайте соответствующий UI в админке.

## Устранение неполадок

### Ссылка не сохраняется

1. Проверьте, что таблица `settings` создана в Supabase
2. Убедитесь, что политики RLS настроены правильно
3. Проверьте консоль браузера на наличие ошибок

### Ссылка не отображается на странице успеха

1. Проверьте, что ссылка сохранена в админке
2. Очистите кэш браузера
3. Проверьте консоль на ошибки загрузки данных

### Ошибка доступа к базе данных

Убедитесь, что:
- Supabase клиент правильно настроен
- Переменные окружения `.env.local` содержат правильные ключи
- Политики RLS разрешают доступ к таблице `settings`
