-- Миграция существующих администраторов из таблицы admins в Supabase Auth
-- ВНИМАНИЕ: Этот скрипт нужно выполнить ТОЛЬКО ОДИН РАЗ после настройки Supabase Auth

-- Шаг 1: Проверьте существующих администраторов
SELECT username, password FROM public.admins;

-- Шаг 2: Для каждого администратора:
-- 1. Зарегистрируйте его через /admin/login (вкладка "Реєстрація")
--    Используйте email вместо username
-- 2. Затем выполните команду ниже для каждого email:

-- Пример:
-- UPDATE public.profiles 
-- SET is_admin = TRUE 
-- WHERE email = 'admin@example.com';

-- Шаг 3: После миграции всех администраторов, можно удалить старую таблицу
-- (ОПЦИОНАЛЬНО, выполняйте только когда уверены, что все работает)
-- DROP TABLE IF EXISTS public.admins;

-- Примечание: Старые пароли из таблицы admins НЕ МОГУТ быть перенесены,
-- так как они хранились в plain text. Администраторы должны создать новые пароли
-- через форму регистрации. Это повышает безопасность системы.
